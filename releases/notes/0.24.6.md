## 新增
- 「讓終端機處理 Claude 的提問」設定（預設關閉）：開啟後 Claude Code 的 AskUserQuestion 不再被 Island 攔截，改由終端機原生選單作答；切換時自動重寫 `~/.claude/settings.json` 的 hook matcher，下一個 Claude session 生效。

## 修復
- 無 `tool_use_id` 的 AskUserQuestion 提問卡卡死無法清除（殭屍卡）：來源無 id 的卡片在該工具完成時直接清除，帶 id 的卡片維持嚴格比對，不影響並行提問。
