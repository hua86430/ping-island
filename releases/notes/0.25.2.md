# v0.25.2

## 新增

- automatic 螢幕模式下，docked 島會跟著游標移動到另一個螢幕：游標在另一螢幕停留約 0.1 秒即遷移，直接重新定位既有視窗、沒有重建閃爍。
- 系統閒置時，hover 也能展開與收合 docked 島。先前只有在有 agent 正在執行或有待處理事項時 hover 才有效，閒置時只能點擊；現在改用專屬的 hover 感測，不再依賴省電時被關閉的全域滑鼠監聽。

## 修復

- hover 展開 docked 島時不再搶走終端機的鍵盤焦點：滑鼠移上去預覽、移開後可直接繼續打字，不用再點一下終端機。只有點擊或快捷鍵展開才會取得焦點。
