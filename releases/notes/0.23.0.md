<!-- zh-Hans -->
## 修复
- 修复 Codex 多选/问题介入时误播放“开始处理”音效的问题，现在会正确播放“需要介入”提示。
- 修复 Codex 任务完成后部分 idle 快照不会触发“完成”音效的问题。
- 保持 Codex hook 侧介入状态在 app-server 刷新期间不被过早清除，降低声音和 UI 状态错位的概率。

## 亮点
- 终端可见时的审批/问题提示继续保持活跃，避免用户错过需要处理的 Codex 会话。
- hook 调试日志现在有边界，诊断数据不会无限增长。

## 说明
- Sparkle build 号提升到 `58`，版本号提升到 `0.23.0`。
- 本次基于最新 `main` 发布，包含 Codex 声音状态修复和近期提示可见性改进。

## 关联 PR / Issue
- `#237` codex声音错误

<!-- en -->
## Fixes
- Fixed Codex multi-choice/question interventions playing the “start processing” sound instead of the “needs attention” cue.
- Fixed completed Codex tasks that settled as idle snapshots without reliably playing the completion sound.
- Preserved Codex hook-side intervention state across app-server refreshes so sound cues and UI state stay aligned.

## Highlights
- Approval/question prompts remain live while the terminal is visible, reducing missed Codex sessions that need user input.
- Hook debug logs are now bounded so diagnostics cannot grow without limit.

## Notes
- The Sparkle build number is now `58`, and the app version is now `0.23.0`.
- This latest-`main` release includes Codex sound-state fixes and recent prompt visibility improvements.

## Related PRs / Issues
- `#237` codex声音错误
